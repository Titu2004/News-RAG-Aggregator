# 📰 News Article Aggregator using Simple RAG

This is a beginner-friendly project that collects news articles from multiple sources and retrieves the most relevant news based on user queries using a simple Retrieval-Augmented Generation (RAG) approach.

---

## 🚀 Features

- Fetches live news from RSS feeds (BBC, TechCrunch, etc.)
- Groups news into basic topics (AI, Mobile, Other)
- Retrieves the most relevant article using TF-IDF similarity
- Generates a simple summary of the retrieved article

---

## 🛠 Technologies Used

- Python
- RSS Parsing (feedparser)
- TF-IDF Vectorization (scikit-learn)
- Cosine Similarity Search

---

## ▶️ How to Run

### 1. Install dependencies

```bash
pip install -r requirements.txt
